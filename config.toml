# The URL the site will be built for
base_url = "https://words.knowlerkno.ws"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "after-dark"

taxonomies = [
    # You can enable/disable RSS
    {name = "categories", rss = true},
    {name = "tags", rss = true},
]

[extra]
# Put all your custom variables here
after_dark_menu = [
    {url = "$BASE_URL", name = "index"},
    {url = "$BASE_URL/tags", name = "tags"},
    {url = "https://knowlerknow.ws", name = "my main site"},
    {url = "https://twitter.com/knowler", name = "twitter"},
    {url = "https://github.com/knowler", name = "github"},
    {url = "https://hire.knowlerkno.ws", name = "resume"},
]

after_dark_title = "words knowler knows"
